# Do not commit .env. The database name may be any local PostgreSQL database.
DATABASE_URL=postgresql://localhost/climate
# Optional: keep the personal account database outside the default ignored path.
USER_DATABASE_PATH=static/users.db
